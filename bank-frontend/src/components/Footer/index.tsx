import React from 'react';
import classes from './Footer.module.scss';

const Footer: React.FC = () => (
  <footer className={classes.root}>
    <img src="/img/logo_pix.png" alt="The Code Pix logo" />
  </footer>
);

export default Footer;
